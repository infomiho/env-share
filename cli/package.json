{
  "name": "@infomiho/env-share",
  "version": "0.4.1",
  "description": "CLI for env-share — self-hosted encrypted .env sharing",
  "repository": {
    "type": "git",
    "url": "https://github.com/infomiho/env-share.git",
    "directory": "cli"
  },
  "type": "module",
  "bin": {
    "env-share": "./dist/cli.mjs"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^13"
  },
  "devDependencies": {
    "typescript": "^5",
    "tsdown": "^0",
    "@types/node": "^22"
  }
}
