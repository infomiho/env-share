DB_PASSWORD=change-me
# For local dev, set the GitHub OAuth App's callback URL to http://localhost:3000/web/callback
# For production, use a separate GitHub OAuth App with the production callback URL
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
COOKIE_SECRET=change-me-to-random-string
