DB_PASSWORD=change-me
# GitHub OAuth App must have the callback URL set to <your-domain>/web/callback
# The same app works for both CLI (device flow) and web (authorization code flow)
# For local dev: http://localhost:3000/web/callback
# For production: https://your-domain.com/web/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
COOKIE_SECRET=change-me-to-random-string
